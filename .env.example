# Database (Neon PostgreSQL)
DATABASE_URL=postgresql://username:password@your-neon-hostname.neon.tech/dbname?sslmode=require

# JWT Secret (use a strong random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# SMTP Email Configuration
# Development: Uses MailHog (no config needed, runs on localhost:1025)
# Production: Configure your SMTP provider
SMTP_HOST=smtp.yourmailprovider.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
SMTP_FROM=noreply@yourdomain.com
SMTP_SECURE=false
SMTP_REJECT_UNAUTHORIZED=true

# S3-Compatible Storage Configuration (Backblaze B2, AWS S3, etc.)
# Optional - only needed if you want to use file uploads
S3_ENDPOINT=https://s3.us-west-004.backblazeb2.com
S3_REGION=us-west-004
S3_ACCESS_KEY_ID=your-access-key-id
S3_SECRET_ACCESS_KEY=your-secret-access-key
S3_BUCKET_NAME=your-bucket-name

# Site URL
NUXT_PUBLIC_SITE_URL=http://localhost:3000

# Node Environment
NODE_ENV=development
